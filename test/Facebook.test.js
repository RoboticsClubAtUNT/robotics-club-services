'use strict';

let should = require('chai').should();

describe('Facebook', () => {
  describe('#post()', () => {
    it("should generate a post and send it to the Robotics Club's Facebook group");
  });
  describe("#event()", () => {
    it("should generate an event on the Robotics Club's Facebook group");
  });
});
