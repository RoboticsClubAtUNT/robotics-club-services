'use strict';

let should = require('chai').should();

describe('Gmail', () => {
  describe('#sendToOne()', () => {
    it("it send an email to a single reciepient");
  });
  describe("#sendToMany()", () => {
    it("it send an email to a many reciepient");
  });
});
